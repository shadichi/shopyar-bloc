import 'package:flutter/material.dart';

class VariationBottomSheet extends StatelessWidget {
  const VariationBottomSheet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

